# Warzone Server Environment Variables
# Copy this to .env and modify as needed (or set these in your hosting platform)

# Port for HTTP and WebSocket server (most hosting platforms set this automatically)
PORT=8000

# Environment (set to "production" when deploying)
NODE_ENV=development
